spring:
  application:
    name: ai-agent
  # application.yml
  profiles:
    active: local

  datasource:
    url: jdbc:postgresql://rm-cn-ai54sx8rs00012ro.rwlb.rds.aliyuncs.com:5432/ai_agent
    username: my_user
    password: 20050828zxhH_
  ai:
    vectorstore:
      pgvector:
        index-type: HNSW
        dimensions: 1536
        distance-type: COSINE_DISTANCE
        max-document-batch-size: 10000

server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: org.example.aiagent.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
search-api:
  apikey: mRB5yLiMcMhrdX92Vc5QLsDV